import React from 'react';
import { IonApp, setupIonicReact } from '@ionic/react';

setupIonicReact();

const App: React.FC = () => (
  <IonApp>
    {/* Routes and navigation will be configured in Task 7 */}
  </IonApp>
);

export default App;
